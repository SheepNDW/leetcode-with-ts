## 3418. Maximum Amount of Money Robot Can Earn

給定一個 `m × n` 的矩陣 `coins`，機器人從左上角 `(0, 0)` 走到右下角 `(m-1, n-1)`，每次只能向右或向下移動。機器人最多可以使用 **2 次**特殊技能，讓某個格子的負值不計入總和。求機器人能獲得的最大金幣數。

### 思路：3D DP

`dp[i][j][k]`：機器人走到 `(i, j)`，且已使用 `k` 次特殊技能時，能獲得的最大金幣數。

多用一次技能永遠不會更差——對正數格子使用技能時，用 `Math.max(val, 0)` 讓收益至少為 0，等同於「白用一次」也不虧。因此 `k=2` 的狀態一定 `>= k=1 >= k=0`，**最終答案直接取 `dp[m-1][n-1][2]`**。

#### 轉移方程

**`k = 0`（從未使用技能）：**
```
dp[i][j][0] = max(dp[i-1][j][0], dp[i][j-1][0]) + coins[i][j]
```

**`k >= 1`（可選擇是否對當前格無效化）：**
```
dp[i][j][k] = max(
  max(dp[i-1][j][k],   dp[i][j-1][k])   + coins[i][j],       // 不使用技能
  max(dp[i-1][j][k-1], dp[i][j-1][k-1]) + max(coins[i][j], 0) // 使用技能
)
```

#### 初始化

**起點 `(0, 0)`：**
```ts
dp[0][0][0] = coins[0][0];
dp[0][0][1] = Math.max(coins[0][0], 0); // 可無效化起點
dp[0][0][2] = Math.max(coins[0][0], 0);
```

**第一 row / 第一 col：** 來源只有單一方向，套用相同的轉移方程即可。

### 複雜度

- **時間複雜度：** `O(m × n × 3)` = `O(m × n)`
- **空間複雜度：** `O(m × n × 3)` = `O(m × n)`
